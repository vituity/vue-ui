const state = {
  fetching: true
}

const mutations = {
  SET_FETCHING (state, isFetching) {
    state.fetching = isFetching
  }
}

const actions = {

}

export default {
  state,
  mutations,
  actions
}
